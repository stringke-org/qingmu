import { IconSettings } from '~/icons';

import { MenuButton } from './menu-button';

export function SettingsButton() {
    return (
        <MenuButton>
            <IconSettings />
        </MenuButton>
    );
}
